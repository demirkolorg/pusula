// ADR-0023 — Tiptap UI public API. Server-safe helper'lar için `lib/tiptap`
// (schema, serialize, mention) — bu barrel sadece client-only bileşenler.

export { TiptapEditor } from "./tiptap-editor";
export { tiptapExtensionleri } from "./extensions";
