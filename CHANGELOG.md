# Değişiklik Günlüğü

[Keep a Changelog](https://keepachangelog.com/tr/1.1.0/) formatına uyulur. Sürümler [Semantic Versioning](https://semver.org/lang/tr/).

## [Yayınlanmamış]

### Değişen
- **Aktivite akışından bildirim altyapısı kayıtları gizlendi** — `lib/aktivite/gizli-kaynaklar.ts` helper'ı: `aktiviteAkisiGorunurKaynakWhere()` Prisma where koşulu `Bildirim`, `BildirimTercih`, `BildirimMailKuyrugu`, `KartSusturma`, `ProjeSusturma` kaynak tiplerini filtre dışı bırakır. Bu kayıtlar audit'te tutulmaya devam eder (forensik gereksinim) ama kullanıcı odaklı aktivite günlüğü, ana sayfa "Son Aktiviteler" widget'ı ve proje aktivite listesi bu altyapı kayıtlarını **göstermez** (gürültü temizliği). `aktivite-gunlugu/services.ts`, `ana-sayfa/services.ts`, `projeler/[projeId]/aktivite/components/aktivite-listesi.tsx`, ana sayfa son aktiviteler bileşenleri ve aktivite detay diyaloğu helper'ı tüketir. 3 unit test (`gizli-kaynaklar.test.ts` — gizli/görünür ayırma + Prisma where üretimi). Forensik denetim sayfası (`/ayarlar/denetim`, ADR-0027) bu filtreyi uygulamaz; ham audit yüzeyinde tüm kayıtlar görünür.
- **Hata logu erişimi yalnızca SUPER_ADMIN'e sınırlandı (ADR-0029)** — `/ayarlar/hata-loglari` sayfası ve `hataListele` + `hataCozumIsaretle` action'ları artık sadece `SUPER_ADMIN` rolünde erişilebilir. KAYMAKAM rolü varsayılan izinlerinden `HATA_LOGU_OKU` ve `HATA_LOGU_COZULDU_ISARETLE` çıkarıldı; sidebar `Hata Logları` menüsü ADR-0024 görünürlük katmanı sayesinde KAYMAKAM'a artık görünmez. Hata logu içeriği (request body, başlıklar, stack trace, internal yol yapısı) idari değil teknik forensik veridir; ADR-0027'nin denetim sayfasını SUPER_ADMIN'e sınırlayan kararıyla aynı doğrultudadır. Üretim ortamında seed yeniden çalıştırılınca KAYMAKAM rolünden iki `RolIzni` satırı temizlenecektir. Bonus: `hataCozumIsaretle` action'ında yanlış izinle (`HATA_LOGU_OKU`) yapılan yetki kontrolü doğru izne (`HATA_LOGU_COZULDU_ISARETLE`) bağlandı (RBAC tutarlılığı, Kural 50).
- **Onay bekleyen kayıtlar kullanıcılar sayfasına entegre edildi (ADR-0025)** — `/ayarlar/onay-bekleyenler` ayrı sayfası kaldırıldı; self-register ile gelen `BEKLIYOR` kullanıcılar artık `/ayarlar/kullanicilar` DataTable'ında "Onay Bekliyor" durum etiketi ile görünür. Satır içi `Onayla` ve `Reddet` butonları (yetki: `KULLANICI_ONAYLA`); reddetme için sebep dialog'u. `useOptimisticMutation` ile anında durum geçişi, hata olursa rollback + toast (Kural 107-116). `REDDEDILDI` satırlarda red sebebi italik gösterilir. Sidebar'dan "Onay Bekleyenler" menüsü kaldırıldı; `KULLANICI_ONAYLA` izni artık `Kullanıcılar` menüsünü tetikler (ADR-0024 K2 güncellemesi). Eski URL `/ayarlar/onay-bekleyenler` redirect stub'ı (`/ayarlar/kullanicilar`'a) — yer-imi kırmaz.
- **Bekleyenler keşfedilebilirliği (ADR-0025/K6)** — Liste sıralaması `onay_durumu ASC` ile başlar → BEKLIYOR'lar listenin başında, alfabetik sıralı kalabalık arasında kaybolmaz. Sayfa başlığında `Onay Bekleyen [N]` sayaç chip'i (sadece `KULLANICI_ONAYLA` yetkili + N > 0 ise) — tıklanınca `onay_durumu='BEKLIYOR'` filtresi devreye girer, tekrar tıklayınca kalkar.
- `app/(panel)/ayarlar/kullanicilar/durum-helper.ts` — saf durum hesaplayıcı (`kullaniciDurumu` + `DURUM_LABEL`); 6 unit test (Kural 139).
- `app/(panel)/ayarlar/kullanicilar/services.ts` — `KullaniciSatiri` tipi `onay_durumu/red_sebebi/onay_zamani` ile genişletildi; kullanılmayan `bekleyenKullanicilariListele` fonksiyonu silindi. `kullanicilariListele` artık `bekleyenSayisi` döner; `orderBy` `onay_durumu ASC` ile başlar; opsiyonel `onay_durumu` filtresi eklendi.
- `app/(panel)/ayarlar/kullanicilar/schemas.ts` — `kullaniciListeSemasi`'ne `onay_durumu: z.enum([...]).optional()` eklendi.
- `app/(panel)/ayarlar/kullanicilar/components/kullanicilar-istemci.tsx` — Durum kolonu (4 badge varyantı) + bekleyen satırlarda Onayla/Reddet butonları + red sebebi `AlertDialog` + optimistic mutation'lar.
- `app/(panel)/ayarlar/kullanicilar/page.tsx` — `onaylayabilir` izni server-side hesaplanıp `Yetkiler`'e iletilir.
- `app/(panel)/ayarlar/kullanicilar/actions.ts` — Kullanılmayan `bekleyenKullanicilariListeleEylem` kaldırıldı; `kullaniciOnaylaEylem` ve `kullaniciReddetEylem` `revalidatePath("/ayarlar/kullanicilar")` ile sadece kullanıcılar sayfasını tazeler.
- `lib/sidebar-yetki.ts` — `AYAR_ONAY_BEKLEYENLER` menü kodu kaldırıldı; `AYAR_KULLANICILAR` haritası `KULLANICI_ONAYLA` ile genişletildi.
- `lib/sidebar-yetki.test.ts` — 4 izinli OR testi `KULLANICI_ONAYLA` dahil edecek şekilde güncellendi.
- `tests/e2e/kayit-onay-giris.e2e.ts` — Onay akışı `/ayarlar/kullanicilar` sayfası üzerinden test edilir (arama + satır içi Onayla + optimistic durum geçişi).

### Değişen
- **Dosya yönetimi modülü F8 — çöp kutusu Dosya'ya bağlandı + realtime sabitleri** (ADR-0028). `app/(panel)/cop-kutusu/services.ts`'in `eklenti` tip'i artık eski `Eklenti` tablosu yerine yeni `Dosya` tablosunu listeler/yönetir; F2 backfill ile silinmiş eklentiler zaten Dosya'ya taşındı, F4 sonrası yeni yazımlar da Dosya'ya gidiyor. Çöp kutusu UI tarafı `tip: "eklenti"` çağrısını koruyor (geriye dönük uyum) ama içerik tamamen yeni modelden. Listeleme: `db.dosya.findMany({ silindi_mi: true })` + `DosyaBaglantisi` üzerinden kapsam filtresi (kullanıcının erişebildiği projeler) + kart başlıklarını batch fetch (denormalize `kart_id` üzerinden). Geri yükle: `dosya-services.geriYukle()` çağırır — `canDosya("dosya:restore")` ile gizlilik + kaynak erişimi + yükleyen ayrımını otomatik yapar. Kalıcı sil: `dosya-services.kaliciSil()` — tüm sürüm storage objelerini de siler (idempotent, başarısız olursa orphan GC). Yeni realtime sabitleri `lib/socket-events.ts`'e eklendi: `DOSYA_OLUSTUR`, `DOSYA_GUNCELLE`, `DOSYA_SIL`, `DOSYA_GERI_YUKLE`, `DOSYA_BAGLANTI_EKLE`, `DOSYA_BAGLANTI_KALDIR`, `DOSYA_ONIZLEME_HAZIR` (servis tetikleyicileri F9'da bağlanacak). Aktivite Türkçe anlatı, bildirim tipleri ve global arama entegrasyonları F8b/F9'a bırakıldı (scope sınırlama). Tüm 873 test geçiyor.

### Eklenen
- **Dosya yönetimi modülü F7 — önizleme paneli + sürüm sekmesi** (ADR-0028). Detay çekmecesi 4 sekmeli yapıya kavuştu (`Önizleme | Bağlantılar | Sürümler | Bilgiler`); aktif dosya değişince sekme `onizleme`'ye sıfırlanır. **`dosya-onizleme-paneli.tsx`**: `lib/dosya-onizleme.ts`'in `onizlemeStratejisi` kararına göre tür bazlı render — `GORSEL_VIEWER` (signed URL ile `<img>`, max-h 60vh contain), `PDF_IFRAME` (sandbox `allow-scripts allow-same-origin`), `METIN_PLAIN` (server fetch + 1MB truncate uyarısı + `<pre>` whitespace-pre-wrap), `MARKDOWN_SANITIZE` (F7'de plain text — DOMPurify F8'de), `OFIS_INDIR/ARSIV_INDIR/INDIR_FALLBACK` (ikon + bilgilendirme + indir butonu). KARANTINA/YUKLENIYOR/HATALI durumdaki dosyalar `lib/dosya-onizleme.ts` mantığıyla zorla `INDIR_FALLBACK` döner. **`dosya-surum-listesi.tsx`**: sürüm listesi (badge ile `v1·aktif` işareti, ad/tarih/boyut/açıklama) + "Yeni Sürüm" butonu (file input → 2-aşamalı upload `dosyaSurumYuklemeBaslatEylem` + `dosyaSurumYuklemeOnaylaEylem` + cache invalidate); aktif sürümde indir butonu. Yeni service: `metinIcerikGetir(kullaniciId, dosyaId)` — `dosya:preview` yetkisi + `kategori=METIN` kontrolü + presigned download → fetch → 1MB truncate + UTF-8 decode + `DosyaErisimLogu` ONIZLEME kaydı. Yeni schema/action: `dosyaMetinIcerikSemasi`/`dosyaMetinIcerikEylem`. Detay çekmecesinde başlık alanı sadeleşti (ad/açıklama inline edit + tek satır metadata: kategori badge · boyut · yükleyen · tarih); aksiyonlar (İndir/Sil/Geri Yükle/Kalıcı Sil) ayrı bileşene taşındı. Filtre çubuğunda kategori ve sıralama Select'leri seçili değer için Türkçe etiket gösterir (`SelectValue` render fonksiyonu). Tüm 873 mevcut test geçiyor.

### Eklenen
- **Dosya yönetimi modülü F6 — merkezi `/dosyalar` sayfası MVP** (ADR-0028). Kullanıcı odaklı dosya yöneticisi: liste + filtre + detay çekmecesi. Sidebar'a "Dosyalar" menü kodu (`MENU_KODLARI.DOSYALAR`, `DOSYA_OKU` izin gereksinimi, `FilesIcon`, `/dosyalar` URL). Server component (`page.tsx`): oturum/izin kontrolü + ilk sayfa SSR; client orchestrator (`dosyalar-istemci.tsx`): filtre state + URL bind (`router.replace` ile paylaşılabilir link), responsive (mobile <640px → kart-list, ≥640px → tablo, `useMobil()` hook'u Kural 16), detay çekmecesi (`ResponsiveDialog` — mobile bottom sheet, desktop center modal Kural 13). Filtre çubuğu (`dosya-filtre-cubugu.tsx`): debounced arama (250ms), tür/sıralama selectleri, çöp kutusu toggle, "Temizle (N)" butonu (44px hit target Kural 11). Tablo (`dosya-tablo.tsx`): ad/tür/yükleyen/tarih/boyut/gizlilik kolonları, satır seçimi → çekmece. Mobil kart listesi (`dosya-mobil-kart-listesi.tsx`): button-based satırlar (a11y), kategori + gizlilik badge'leri. Detay çekmecesi (`dosya-detay-cekmecesi.tsx`): inline ad/açıklama düzenleme (click-to-edit), metadata grid (tür, boyut, MIME, durum, yükleyen, yüklenme, indirme sayısı, hash), bağlı kaynaklar listesi, sürüm listesi (varsa), aksiyonlar (İndir/Sil/Geri Yükle/Kalıcı Sil). Hooks: `useDosyaListesi` (TanStack `useInfiniteQuery`, cursor pagination), `useDosyaDetay` (`useQuery`), 6 mutation (`useDosyaSil/GeriYukle/KaliciSil/AdGuncelle/AciklamaGuncelle/GizlilikGuncelle`) `useOptimisticMutation` ile (Kural 107-116) — sayfalı liste için cursor-aware optimistic update helper'ı (filtreyi tüm `pages`'e uygular). Saf helper: `helpers/dosya-filtre.ts` (URL search params ↔ filtre obje dönüşümü, default temiz URL, aktif filtre sayacı, boyut formatı) + 21 unit test. Tüm 873 test geçiyor.

### Değişen
- **Dosya yönetimi modülü F5 — kart eklenti UI yeni `dosyalar` modülüne bağlandı** (ADR-0028). Kart modalındaki "Ekler" listesi ve `eklenti-paneli.tsx` artık eski `Eklenti` tablosuna yazmaz; `app/(panel)/projeler/[projeId]/eklenti/` modülü compatibility wrapper olarak yeniden yazıldı: action'lar yeni `app/(panel)/dosyalar/services.ts` fonksiyonlarına delege eder (`yeniListele`, `yeniYuklemeBaslat`, `yeniYuklemeOnayla`, `yeniIndirUrl`, `yeniSil`). Upload akışı 2-aşamalı kalır ama 2. adımda `kart_id + ad + mime + boyut + depolama_yolu` yerine sade `kart_id + oturum_id` taşınır (sunucu oturumdan tüm bilgiyi okur). UI `EklentiOzeti` tipini bekleyecek şekilde tutuldu — yeni `DosyaListeSatiri` formatı service katmanında `EklentiOzeti`'ye dönüştürülür (UI değişikliği minimum). Eski `Eklenti` tablosu F2 backfill ile dolu, F4 sonrası yazım durdu, F8/F10'da kaldırılacak. Eski `services.test.ts` silindi (yeni davranış `dosyalar/services.test.ts`'te 10 test ile kapsanıyor).

### Eklenen
- **Dosya yönetimi modülü F4 — service + action katmanı** (ADR-0028). 5 katmanlı modül yapısı (Kural 90) tamamlandı: `app/(panel)/dosyalar/{schemas,services,actions}.ts` + README. **18 Zod şeması** (liste filtresi cursor pagination, upload başlat/onayla, sürüm başlat/onayla, indir/önizle, ad/açıklama/gizlilik güncelle, etiket ata/oluştur/sil, bağlantı ekle/kaldır, sil/geri-yükle/kalıcı-sil). **19 server action** `eylem()` wrapper ile (audit context + hata logu + request_id otomatik); upload action'larında `uploadLimiter` (10/dk/kullanıcı). **services.ts** business logic: 2-aşamalı upload (oturum → presigned PUT → onayda Dosya+Sürüm+Bağlantı tek transaction; storage boyut stat ile doğrulama); kapsam-aware listele (makam tüm + personel sadece erişilebilir kaynaklar + kendi orphan'ları); detay (sürüm + etiket + bağlantı + erişim sayacı); indir/önizle (DosyaErisimLogu kaydı + indirme sayacı); metadata mutation'ları (ad güncelleyince uzantı yeniden hesaplanır); etiket CRUD (oluştur/sil/dosyaya ata-değiştir); bağlantı ekle/kaldır (hedef kaynakta edit yetkisi); sürüm yükleme (mevcut Dosya'ya yeni sürüm + aktif_surum_id güncelle); soft delete + geri yükle + kalıcı sil (storage objelerini de siler, idempotent). 10 integration test (kapsam, filtre, soft delete, metadata, etiket, bağlantı). Tüm 856 test geçiyor. F5'e geçmeye hazır.
- **Dosya yönetimi modülü F3 — storage + güvenlik + yetki + önizleme helper'ları** (ADR-0028). Service/action katmanına geçmeden önce dört bağımsız altyapı modülü:
  - `lib/dosya-storage.ts` — yeni `pusula-dosyalar` MinIO bucket'ı (Eklenti'den ayrı, env: `MINIO_DOSYA_BUCKET`); yol formatı `dosyalar/<yyyy>/<mm>/<dosyaId>/<surumNo>/<rastgele>.<ext>` (yıl/ay segmentasyonu = lokalize listing); `dosyaYoluUret` saf üreteç (UTC, 8 char uzantı kırpma, özel karakter temizliği); `dosyaYoluGuvenliMi` path traversal koruması (`..`, leading slash, control char, `dosyalar/` prefix zorunlu); `presignedDosyaUpload` (5dk TTL), `presignedDosyaDownload` (10dk TTL), `dosyaObjesiniSil`, `dosyaObjesiBoyutuAl`, `dosyaObjesiVarMi` orphan check.
  - `lib/dosya-guvenlik.ts` — MIME whitelist (kategori-eşlemeli); kategori bazlı boyut limitleri (Görsel 25MB, PDF/Office 50MB, Metin 10MB, Arşiv 100MB, Diğer 25MB); `mimeUzantiTutarliMi` (örn. `application/pdf` + `.docx` reddi); `magicByteEsleseMi` 9 binary format için signature kontrolü (PDF, PNG, JPEG, GIF87a/89a, WebP, ZIP, 7z, gzip, OOXML, eski Office CFB); `kategoriMimeIcinUygunMu`; `uploadGirdisiniDogrula` tek noktadan validation (alan + sebep + Türkçe mesaj).
  - `lib/dosya-yetki.ts` — Resource-level RBAC (Kural 50 + 146): `canDosya(kullaniciId, aksiyon, dosyaId)` ve `yetkiZorunluDosya` (throw versiyonu). 12 aksiyon: oku/indir/önizle/edit-meta/edit-gizlilik/tag/link-add/link-remove/version-add/delete/restore/purge. Karar mantığı: makam (SUPER_ADMIN/KAYMAKAM) tüm filtreleri aşar; diğer roller dosyanın bağlı olduğu kart/proje/liste'den birine erişim varsa görür; bağlantısız "orphan" dosya yalnız yükleyen + makam; gizlilik (NORMAL hepsi, HASSAS BIRIM_AMIRI+, GIZLI yükleyen + makam); kalıcı silme yalnız makam; soft-deleted dosya sadece read/restore/purge.
  - `lib/dosya-onizleme.ts` — Tür bazlı render stratejisi: 7 enum değeri (`GORSEL_VIEWER`, `PDF_IFRAME`, `METIN_PLAIN`, `MARKDOWN_SANITIZE`, `OFIS_INDIR`, `ARSIV_INDIR`, `INDIR_FALLBACK`); KARANTINA/YUKLENIYOR/HATALI durumdaki dosyalar zorla `INDIR_FALLBACK` (önizleme yok); SVG inline render YASAK (Kural 70 XSS) → `INDIR_FALLBACK`; markdown `MARKDOWN_SANITIZE` (DOMPurify zorunlu); Office (Word/Excel/PowerPoint) `OFIS_INDIR` (PDF conversion v2). `inlineOnizlemeMi` UI helper'ı.
  - 4 unit test dosyası + 1 integration test (yetki) = **89 yeni test** (storage 14, güvenlik 25, önizleme 24, kategori 16, yetki 10). Tüm 841 mevcut test geçiyor.
- **Dosya yönetimi modülü F2 — Eklenti → Dosya backfill script** (ADR-0028). `prisma/scripts/backfill-eklenti-dosya.ts` her `Eklenti` kaydını idempotent şekilde `Dosya` + `DosyaSurumu(surum_no=1)` + `DosyaBaglantisi(kaynak_tip=KART)` üçlüsüne taşır. `Dosya.id = Eklenti.id` (aynı UUID, `WHERE NOT EXISTS` ile idempotency); tek transaction her Eklenti için yarım state olmaz; bucket `lib/storage.ts` BUCKET sabitinden okunur; silindi_mi/silinme_zamani/olusturma_zamani Eklenti'den kopyalanır (tarihçe bütünlüğü). Audit middleware bu script'te devre dışı (kasıtlı: backfill gerçek CREATE değil, taşıma; eski Eklenti audit'i kayıtlı). Çalıştırma: `bun run prisma/scripts/backfill-eklenti-dosya.ts`. Saf MIME→kategori helper'ı `lib/dosya-kategori.ts` (Word→OFIS_BELGESI, Excel→TABLO, PowerPoint→SUNUM eşlemesi; MIME yoksa uzantıya düşer; her ikisi de tanınmıyorsa DIGER) ve Türkçe etiket sözlüğü `DOSYA_KATEGORI_ETIKETI` eklendi. 16 unit test (helper) + 5 integration test (backfill: aktif dosya, silinmiş dosya, idempotency, çoklu kayıt, boş tablo).
- **Dosya yönetimi modülü F1 — Prisma schema + migration + izin kataloğu** (ADR-0028). 6 yeni enum (`DosyaKategori`, `DosyaDurumu`, `DosyaGizlilik`, `DosyaKaynakTipi`, `DosyaIslemeDurumu`, `DosyaErisimTipi`) + 7 yeni model (`Dosya`, `DosyaSurumu`, `DosyaBaglantisi`, `DosyaEtiketi`, `DosyaEtiketBaglantisi`, `DosyaYuklemeOturumu`, `DosyaErisimLogu`) eklendi. `IzinKategorisi` enum'una `DOSYA` değeri eklendi (Postgres `ADD VALUE` semantiği gereği ayrı migration). 19 yeni `DOSYA_*` izni izin kataloğuna işlendi: temel (oku/yükle/indir/önizle), düzenle (ad/açıklama/gizlilik), etiket (ata/yönet), bağlantı (ekle/kaldır), sürüm (yükle), silme (kendi/başka/geri-yükle/kalıcı), toplu (toplu-işlem/dışa-aktar) ve güvenlik (güvenlik-yönet). Rol atamaları: KAYMAKAM/SUPER_ADMIN tüm 19 izni; BIRIM_AMIRI 17 (kalıcı sil + güvenlik yönet hariç); PERSONEL 10 (temel + ad/açıklama düzenle + etiket-ata + bağlantı-ekle + sürüm-yükle + kendi-sil). Dosya tablosuna `arama_vektoru` tsvector trigger'ı eklendi (ADR-0017 desenine uygun, ad=A + aciklama=B; F8'de etiket/metin için zenginleştirilecek). Migration'lar: `20260510000000_dosya_izin_kategori_enum`, `20260510010000_dosya_yonetimi_cekirdek`. `Eklenti` modeli F2 backfill için korunur, F4 sonrası kaldırılır.
- **Dosya yönetimi modülü F0 — ADR-0028 yazıldı** ([docs/adr/0028-dosya-yonetimi-cekirdek-modeli.md](docs/adr/0028-dosya-yonetimi-cekirdek-modeli.md)). `Eklenti` kart-eki modelinden bağımsız `Dosya` çekirdek modeline geçiş kararı; 7 tasarım sorusu sahip onayıyla karara bağlandı: v1 kaynak tipleri **KART + PROJE + LISTE**; gizlilik **NORMAL/HASSAS/GIZLI** üç katmanlı; Office önizleme v1'de indirme fallback (LibreOffice v2); soft delete retention **30 gün**; **ayrı `DosyaEtiketi` tablosu** (kart etiketinden ayrı evrak sınıflama); hash baştan saklanır, dedup UI v2; v1'de ClamAV servisi yok ama schema (`KARANTINA` durumu, `virus_tarama_durumu`) baştan hazır. Plan dosyası ([docs/issues/2026-05-06-dosya-yonetimi-modulu-plani.md](docs/issues/2026-05-06-dosya-yonetimi-modulu-plani.md)) Bölüm 20 cevaplandı, durum "F0 tamamlandı, F1 başlayabilir". Kod F1'de yazılır.
- **Aktivite Günlüğü modülü (ADR-0027)** — Kullanıcı odaklı `/aktivite-gunlugu` sayfası eklendi. Yeni izinler: `aktivite:oku` ve `aktivite:disa-aktar`. Sayfa 5 katmanlı modül yapısını izler: Zod filtre şeması, kapsam-aware servis, Server Action, TanStack infinite query hook'u ve sanallaştırılmış liste UI'ı. Makam kullanıcıları tüm aktiviteleri; diğer roller kendi ve erişebildiği proje/liste/kart bağlamındaki aktiviteleri görür. Ana sayfa "Tüm Aktiviteler" linki artık bu sayfaya gider. `/ayarlar/denetim` ham forensik audit yüzeyi olarak yeniden adlandırıldı ve yalnızca `SUPER_ADMIN` rolüne sınırlandı.
- `lib/aktivite/` — Aktivite anlatısı (`aktiviteAnlati`), kapsam filtresi (`kapsamBaglamiHazirla`, `kapsamWhere`) ve idari kaynak mesajları (`Kullanici`, `Rol`, `Izin`, `Birim`, `DavetTokeni`) için paylaşılan public API. 3 yeni unit test dosyası eklendi.
- **Ana sayfa rol-aware metrikleri + Makam KPI şeridi (ADR-0026)** — Mevcut 4 metrik kartı (Açık Görevim, Geciken, Bu Hafta, Bekleyen Davet) artık kullanıcının yetki kapsamına göre dallanır: PERSONEL/BIRIM_AMIRI **kişisel** (mevcut davranış birebir), KAYMAKAM/SUPER_ADMIN **sistem** (tüm aktif kart, sistem geneli geciken, aktif tüm davet). `AnaSayfaMetrik`'e `kapsam: "kisisel"|"sistem"` ve `bekleyenDavetTum` alanları eklendi. Makam başlıkları: "Açık Görevim" → "Sistemde Açık", "Bekleyen Davet" altyazısı "Sistemde aktif davet". Tekil/çoğul Türkçesi düzeltildi ("1 görev gecikti" / "2 görev gecikti"). Makam ana sayfasında üstte ek **KPI şeridi** (`MakamKpiSeridi`) — Aktif Proje · Aktif Kullanıcı (son 7 gün) · Onay Bekleyen Kullanıcı · Kritik Hata (son 24sa, FATAL/ERROR + çözülmemiş). Servis (`makamKpisiniGetir`) non-makam için null döner; veri sızıntısı sunucu tarafında engellenmiş. 8 yeni unit test (toplam 29 geçiyor).
- **Ana sayfa Son Aktiviteler — proje aktivite mimarisi ile birleşim** — Ana sayfa "Son Aktiviteler" widget'ı artık proje aktivite modülünün olgun `AktiviteOzeti` tipini ve `AktiviteDetayIcerik` UI'ını yeniden kullanıyor (tek kaynak — DRY, Kural 131). Kullanıcıya gösterilen değişimler:
  - **Liste satırı yeniden tasarlandı:** birinci satır kaynak başlığı (kart başlığı / liste adı / proje adı — büyük & öne çıkan), ikinci satır `Proje › Liste` bağlam zinciri (ikonlu chip), üçüncü satır işlem badge (Yeni/Güncelleme/Silme renkli) + kim + olay mesajı. Makam ekrana baktığında "ne, hangi projede, kim" bir bakışta okunur.
  - **Detay modalı user-friendly yapıldı:** Eski JSON ham veri (Yeni Veri / Meta blokları) kaldırıldı; artık proje aktivite modülünün `AktiviteDetayIcerik`'i render ediliyor — Türkçe olay özeti ("Sistem Yöneticisi bir kartın bitiş tarihini değiştirdi"), meta grid (kullanıcı / tarih / işlem / kategori / proje / liste / kart) ve renkli alan diff kartları (eski ↔ yeni, kelime düzeyi vurgu).
  - **Modal genişletildi:** `max-w-3xl` → `max-w-4xl`.
  - **Yorum mention'ları:** server tarafında zaten görünen ada çevriliyor; ana sayfa boş kişi map'i ile çalışır.
- `projeler/[projeId]/aktivite/services.ts` — `zenginlestirVeOzetle` fonksiyonu ve `HamAktivite` tipi `export` edildi (ana sayfa servisi tüketebilsin diye). Davranış aynı.
- `app/(panel)/ana-sayfa/services.ts:sonAktiviteleriGetir` — Eski el-yapımı `kaynakEtiketleriOlustur` çağrısı kaldırıldı; artık ham audit kayıtlarını çekip `zenginlestirVeOzetle()`'e teslim ediyor (~80 satır → ~20 satır).
- `app/(panel)/ana-sayfa/schemas.ts:SonAktiviteSatiri` — Tip artık proje aktivite modülünden re-export ediliyor (`AktiviteOzeti`); ana sayfaya özel detay alanları (eski_veri/yeni_veri/diff/meta/ip/http_*) kaldırıldı.
- **Ana sayfa fit-to-viewport + KarsilamaBaslik kaldırıldı** — Ana sayfa artık scroll vermez; outer panel container yüksekliğine sığar (`h-full min-h-0 flex-col`). Son Aktiviteler kartı kalan dikey alanı kaplar (`flex-1`) ve **kendi içinde scroll** alır (sticky tarih grup başlıkları korunur). Sağ stack (Bana Atanan + Son Ziyaret) da dar viewport'larda kendi içinde scroll alır. "Hoş geldiniz, …" karşılama bloğu tamamen kaldırıldı (`KarsilamaBaslik` bileşeni silindi) — odak doğrudan metrik kartları + Son Aktiviteler.
- **Ana sayfa layout yeniden düzenleme — Son Aktiviteler odak alanına** — `BanaAtananKartlar` ↔ `SonAktiviteler` yer değiştirdi: artık Son Aktiviteler ana 2/3 alanı (sol), sağda dikey stack: Bana Atanan Kartlar üstte + Son Ziyaret Edilen altta. Mobil tek kolon (Aktiviteler en üstte — odak korunur). Aktivite limiti 15 → 25 yükseltildi (geniş alana sığar). Tarih grupları eklendi (Bugün / Dün / Bu Hafta / Daha Eski) sticky başlıklarla — makam için tarama hızı arttı. Bugün/dün satırlarında sadece saat, eski tarihlerde tarih+saat. Denetim okuma izni varsa kart başlığında "Tüm Denetim →" linki (RBAC: `DENETIM_OKU`). Metrik kartları eşit yükseklik (alt yazı `line-clamp-1` + `min-h-4` garantisi).
- `app/(panel)/ana-sayfa/components/son-aktiviteler-helper.ts` — saf grup helper (`zamanGrubu`, `gruplaraAyir`); 9 unit test (Kural 139).
- **Ana sayfa "Son Aktiviteler" zenginleştirme** — Her satır artık kullanıcı + işlem badge (CREATE/UPDATE/DELETE renkli) + kaynak tipi Türkçe etiketi (örn. "Kart oluşturdu", "Proje güncelledi") + kaynak başlığı/adı (örn. "Asfalt Projesi", "Sokak temizliği") + relatif zaman gösterir. Tüm satır tıklanabilir (44px hit target — Kural 11), tıklanınca detay diyalog açılır: değişen alanlar (eski → yeni renkli diff kartları), tam veri (eski/yeni/meta JSON detayları), zaman/kullanıcı/IP/HTTP meta bilgisi. Mobilde bottom sheet, desktop'ta center modal (Kural 13).
- `lib/audit-kaynak-etiket.ts` — Denetim sayfasındaki ~280 satırlık `kaynakEtiketleriOlustur` helper'ı paylaşılan `lib/`'e taşındı (DRY, Kural 131). 16 tablo paralel batch çözümleme. Hem denetim hem ana sayfa kullanır.
- `lib/audit-kaynak-etiket.test.ts` — helper için 10 unit/entegrasyon testi (Kural 139).
- `lib/constants/log.ts` — `KAYNAK_TIP_LABEL` + `kaynakTipEtiketi` (saf TS, client-safe; client componentlerden import edilir).
- `app/(panel)/ana-sayfa/components/aktivite-detay-diyalog.tsx` — Denetim diff diyaloğunun ana sayfa varyantı, `ResponsiveDialog` ile mobil/desktop ayrımı.

### Değişen
- `app/(panel)/ana-sayfa/schemas.ts:sonAktiviteSatiriSemasi` — `kaynak_etiket`, `eski_veri`, `yeni_veri`, `diff`, `meta`, `ip`, `http_metod`, `http_yol`, `request_id` alanları eklendi.
- `app/(panel)/ana-sayfa/services.ts:sonAktiviteleriGetir` — Detay alanları çekilir, `kaynakEtiketleriOlustur` paralel çağrısıyla kaynak etiketi çözülür.
- `app/(panel)/ayarlar/denetim/actions.ts` — Helper kaldırıldı, `lib/audit-kaynak-etiket.ts`'ten import edilir (~280 satır azaltıldı; davranış aynı).
- `app/(panel)/ana-sayfa/services.test.ts` — 4 yeni test eklendi (kaynak etiket çözümleme + detay alanları + null kullanıcı senaryoları).

- **Etiket modülü tam kapsam** — Proje altında `/projeler/[projeId]/etiket` (yönetim) ve `/projeler/[projeId]/etiket/[etiketId]` (detay) sayfaları. Yönetim sayfası proje etiketlerini liste halinde gösterir; satır içi düzenle/sil + yeni oluştur. Detay sayfası etikete bağlı kartları paginated listeler (kart başlığı, liste, bitiş, üye sayısı, son güncelleme; tıklayınca kart modal). Granüler RBAC (Kural 146): `KART_ETIKET_OLUSTUR/DUZENLE/SIL/ATA/CIKAR` izinleri her aksiyona resource-level `proje:edit` ile birlikte uygulanır. CRUD eventleri proje room'una realtime yayınlanır (`SOCKET.ETIKET_OLUSTU/GUNCELLENDI/SILINDI`); başka sekmelerde anlık senkron. Proje başlık sekmelerine "Etiketler" linki eklendi.
- `app/(panel)/projeler/[projeId]/etiket/page.tsx` — yönetim sayfası (server component).
- `app/(panel)/projeler/[projeId]/etiket/[etiketId]/page.tsx` — detay sayfası (etikete bağlı kartlar).
- `app/(panel)/projeler/[projeId]/etiket/components/{etiket-form-dialog,etiket-sil-diyalog,etiket-listesi-istemci,etiket-detay-baslik,etiket-kartlari-listesi}.tsx` — yeni UI bileşenleri.
- `app/(panel)/projeler/[projeId]/etiket/services.ts` — `etiketDetayGetir`, `etiketKartlariniListele` + CRUD'a realtime yayın.
- `app/(panel)/projeler/[projeId]/etiket/actions.ts` — `etiketDetayEylem`, `etiketKartlariEylem` + tüm aksiyonlar granüler izine geçti.
- `app/(panel)/projeler/[projeId]/etiket/hooks.ts` — `useEtiketDetay`, `useEtiketKartlari`, `useEtiketRealtime`.
- `app/(panel)/projeler/[projeId]/etiket/services.test.ts` — yeni 2 fonksiyon için 5 test (toplam 13).
- **Sidebar RBAC görünürlük filtresi (ADR-0024)** — Yan menü artık kullanıcının izinlerine göre filtrelenir. Sayfa erişim güvenliği zaten her sayfada `izinVarMi` + redirect/notFound ile sağlanıyordu (değişmedi); bu katman yalnızca menü görünürlüğünü hizalar. Yetkisiz kullanıcılar artık "Roller", "Hata Logları", "Onay Bekleyenler", "Birimler", "Denetim" gibi yönetim öğelerinin başlıklarını dahi görmez. Boş kalan grup başlığı (`SidebarGroupLabel`) da gizlenir. `lib/sidebar-yetki.ts` saf modül (12 menü kodu, izin haritası, `menuGorunurMu` + `gorunurMenuKodlari`); 14 unit test (`lib/sidebar-yetki.test.ts`). Layout server-side `kullaniciIzinleriniAl` (cache'li) çağrısıyla `AppSidebar`'a `gorunurKodlar` prop'unu iletir.
- **Kart açıklamasında zengin metin (ADR-0023)** — Tiptap (ProseMirror) editör; bold, italik, üstü çizili, satır içi kod, başlık (H1-H3), madde işaretli & numaralı liste, link. shadcn primitive'leri (Tooltip + Popover + Separator) ile özel toolbar; mobil 360px'te yatay scroll. `Kart.aciklama` (String) → `Kart.aciklama_dokuman` (Json) + `Kart.aciklama_metin` (denormalize plaintext, tsvector trigger'ı + line-clamp listesi + audit diff bunu okur). Server-side Zod doc validation (max 1000 düğüm, 10 derinlik, 10K karakter/text). Optimistic mutation `useKartGuncelle` 1500ms onBlur debounce ile çalışır.
- `lib/tiptap/` — server-safe helper kütüphanesi (TiptapDokuman Zod şeması, plaintext serialize, mention çıkarma + 33 unit test).
- `components/tiptap/` — istemci editor + toolbar + link popover.
- `prisma/migrations/20260508000000_kart_aciklama_tiptap_dokuman` — kolon değişimi + tsvector trigger refactor.
- **Makam katmanı bildirim otomatik alıcı (ADR-0022)** — `SUPER_ADMIN` ve `KAYMAKAM` rolündeki aktif/onaylı kullanıcılar `bildirimUret` tek noktasından **tüm bildirim tetikleyicilerine** otomatik alıcı olarak dahil edilir. Kural 50a'nın bildirim katmanına yansıması: makam kullanıcısı atama/üyelik gerektirmeden tüm projelerin akışını görür. Üreten dışlama, `BildirimTercih` ve susturma süzgeçleri korunur (opt-out kullanılabilir). Email kanalı (Adım 4 / Faz 5.5) için de aynı union uygulanır.
- **Genel Arama / Komut Paleti (ADR-0017)** — Cmd/Ctrl+K + Cmd/Ctrl+Space ile açılan global arama. 9 tipte aranabilir: kart, yorum, kontrol maddesi, eklenti, kullanıcı, birim, etiket, proje, liste. Postgres `tsvector` + custom `pusula_turkish` config (Snowball Türkçe stemmer + unaccent + stop-word) + `pg_trgm` fuzzy fallback. UNION ALL sorgu, app-level yetki filtresi (Makam — SUPER_ADMIN/KAYMAKAM — bypass).
- `app/(panel)/genel-arama/` — feature folder (5 katman: schemas, services, actions, hooks, components + saf logic helper + 16 unit test).
- `app/(panel)/genel-arama/components/komut-paleti.tsx` — geniş modal (90vw × 86vh), 4 sekmeli tab bar (Hepsi / İçerik / Yapı / Kişi-Yer), Ctrl+1..4 sekme kısayolu, sayaç pill, durationMs.
- `app/(panel)/genel-arama/components/arama-vurgula.tsx` — Türkçe-duyarsız `<mark>` highlight (çoklu kelime, overlap birleştirme).
- `app/(panel)/genel-arama/components/arama-sonuc-item.tsx` — renkli kategori ikonu (h-12 w-12), tip badge, ChevronRight, tip-spesifik meta.
- `prisma/migrations/20260507000000_global_arama_tsvector_altyapisi` — 9 tabloya `arama_vektoru tsvector` kolonu, 9 trigger fonksiyonu, GIN + trgm index'leri, backfill.
- `components/ui/command.tsx` — shadcn cmdk wrapper (CommandDialog, Input, List, Group, Item, Empty, Separator).
- `cmdk` paketi.
- `lib/rate-limit.ts:aramaLimiter` — 30 sorgu/dk/kullanıcı throttle.
- **Birim paylaşım modeli (ADR-0008)** — `Kurum` → `Birim` rename; `ProjeBirimi`, `ListeBirimi`, `ListeUyesi`, `KartBirimi` join tabloları. Bir kaynak birden çok birime/kişiye paylaşılabilir.
- `app/(panel)/projeler/[projeId]/paylasim.ts` — proje + liste için birim ve üye CRUD servis.
- `app/(panel)/projeler/[projeId]/components/{proje-paylasim-popover,liste-paylasim-popover,birim-paylasim-listesi}.tsx` — paylaşım UI bileşenleri.
- `app/(panel)/ayarlar/birimler/` — birim envanteri yönetimi (eski `kurumlar/` yerine).
- `lib/constants/birim.ts` — birim tip etiketleri.
- `prisma/migrations/20260504165000_birim_paylasim_modeli` — veri koruyan rename + join tablo migration'ı.
- `docs/adr/0008-birim-paylasim-saf-model.md` — saf görünürlük modeli kararı.
- `lib/yetki.ts` — resource-level RBAC (proje/liste/kart için `can*` policy katmanı, `ProjeUyesi` entegrasyonu).
- `lib/rate-limit.ts` — in-memory token bucket (login/davet/upload/log limit).
- `proxy.ts` — Next 16 yeni middleware konvansiyonu (eski `middleware.ts` taşındı).
- `.nvmrc`, `package.json#engines`, `packageManager` — Node 22 LTS + Bun 1.3+ sözleşmesi.
- `.github/workflows/ci.yml` — typecheck + lint + test + build pipeline.
- `CHANGELOG.md` — bu dosya.
- `docs/adr/0002..0005` — mimari audit kararları.
- `docs/issues/2026-05-04-mimari-audit.md` — dış mühendis audit raporu.

### Değişen
- **Saf görünürlük modeli** — `lib/yetki.ts` ve `services.ts` görünürlük where'larında "atama yoksa üst seviyeden devral" fallback'i kaldırıldı. Kaynak yalnızca atanan kişi/birim ve makam kullanıcılarına görünür.
- `Kullanici.birim_id` artık nullable (makam kullanıcılar için).
- Proje/liste/kart `create` çağrılarında oluşturanın birimi otomatik `*Birimi` join tablosuna eklenir.
- `auth.config.ts` — cookie `sameSite: "strict"`.
- `/api/log/hata` — auth zorunlu, origin allowlist, rate limit.
- Proje/liste/kart `services.ts` — tüm fonksiyonlar `kullaniciId` alır, `yetkiZorunluKaynak` çağırır.
- `kullanicilar-istemci.tsx`, `birim-form.tsx` — `useOptimisticMutation` ile sarıldı.
- `useMobile` → `useBreakpoint() === 'mobile'` (tek kaynak).
- Sonner toast position: mobile `top-center`, desktop `top-right`.
- `tsconfig.json` — `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes` açık, `allowJs: false`.
- E2E `tablet-chromium` → `tablet-webkit` rename, `getByRole` semantik locator.
- İngilizce sızıntılar Türkçe (`Projects` → `Projeler`, `More` → `Daha fazla`).
- `docs/plan.md` — Next 15 → Next 16 güncellemesi.
- `README.md` — Pusula'ya özgü yeniden yazıldı (Bun, port 2500).

### Kaldırılan
- `components/nav-projects.tsx` — sidebar'da redundant ("Yönetim", "Denetim" linkleri "Ayarlar" grubunda zaten var). ADR-0024.
- `/ayarlar/etiketler` sidebar linki — sayfa hiç oluşturulmamıştı, ölü link. ADR-0024.
- `lib/constants/kurum.ts`, `app/(panel)/ayarlar/kurumlar/`, `tests/e2e/kurum-crud.e2e.ts`.
- `app/(panel)/projeler/[projeId]/components/kart-hedef-kurumlar.tsx`, `kart-kurum-popover.tsx`, `kart-hedef.ts` (yerine `kart-birim.ts`).
- `Proje.kurum_id` (tekil FK) — yerine çoklu `ProjeBirimi`.

### Düzeltilen
- Drag-drop infinite loop (`useEvent` + memoize edilmiş data + module-level constraints).
- LexoRank "0" alfabe tabanı çarpması — server-side rebalance + retry.
- Drop sonrası soldan akma — `transition: null` iki katmanda.
- Cross-list placeholder eksik — pointer-aware `kartDropKonumuHesapla`.
- Veri kaybı — `baslangicSnapshotRef` + `onDragCancel` manuel rollback.
- Resource-level yetki bypass — `lib/yetki.ts` (KVKK riski kapatıldı).

### Kontrol Skill
- Kural 145 — Major version upgrade sonrası 24 saat içinde plan + README güncel.
- Kural 146 — Resource-level RBAC her aksiyon için zorunlu (`can*` kontrolü).
- Kural 147 — Public POST endpoint'leri auth + rate limit + origin allowlist'siz YASAK.

## [0.1.0] — 2026-05-03 (Initial S0-S3)

### Eklenen
- S0: Next.js 16 + Bun + Prisma + NextAuth + shadcn/ui + TanStack Query/Table altyapı.
- S1: Auth (giriş/kayıt/davet/parola sıfırla) + kullanıcı/birim CRUD.
- S2: RBAC izin matrisi + audit log + hata logu + denetim sayfaları.
- S3: Proje + Liste + Kart CRUD + Trello tarzı kanban + drag-drop + liste görünümü.
- Optimistic UI altyapısı (`useOptimisticMutation` + LexoRank + temp-id).
- Test altyapısı (Vitest + Testing Library + Playwright + 151 test).
- Kontrol skill — 144 mimari kural (`.claude/skills/kontrol/SKILL.md`).
